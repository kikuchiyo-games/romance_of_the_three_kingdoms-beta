require('./spec_helper.js')

describe('Region', function(){
  it('is a createjs object', function(){
    //  cannot figure out how to test with mocha... createjs, document, window dependencies all fail!
  }); 
}); 
